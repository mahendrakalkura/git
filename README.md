How to install?
===============

```
$ git clone --recursive git@github.com:mahendrakalkura/git.git
$ cd git
$ go get
$ go build
```

How to run?
===========

```
$ cd git
$ ./git --directory $HOME
```
